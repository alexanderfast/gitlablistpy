# gitlablistpy

A list of repositories on a gitlab instance

## Dev shell

`nix develop . --command zsh`

## How to run

Define env vars:

* `GITLAB_URL`
* `GITLAB_API_TOKEN`

Run with:

`nix run . projects`
